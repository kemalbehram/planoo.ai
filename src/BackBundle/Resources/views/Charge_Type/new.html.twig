{% extends "::Backend/base.html.twig" %}

{% block content %}
    <h1>ChargeType creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('back_chargeType_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
